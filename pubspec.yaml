name: todo_flutter
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 1.0.0+1

environment:
  sdk: ">=2.15.0 <3.0.0"

dependencies:
  flutter:
    sdk: flutter


  cupertino_icons: ^1.0.2
#  bloc dependency
  flutter_bloc: ^8.1.1
#  hydrated bloc dependency
  hydrated_bloc: ^9.0.0
#  path provider for getting local directory path
  path_provider: ^2.0.11
# equality comparisons
  equatable: ^2.0.5
# To generate to/from JSON code for a class
  json_serializable: ^6.5.4


dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^1.0.0
  build_runner: ^2.3.3

flutter:

  uses-material-design: true